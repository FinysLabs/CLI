import { AgentContext } from '../types';


export default async function price_checker(context: AgentContext) {
  // TODO: implement your agent logic here
  console.log('Agent "price-checker" running with context:', context);
}
